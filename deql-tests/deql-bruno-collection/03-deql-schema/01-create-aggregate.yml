info:
  name: 01-create-aggregate
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/{{deql_org_id}}/dereg/definitions"
  body:
    type: json
    data: '{"statement":"CREATE AGGREGATE SavingsBank;"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
