info:
  name: 03-deql-aggregates
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/{{deql_org_id}}/deql/registry/aggregates"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
