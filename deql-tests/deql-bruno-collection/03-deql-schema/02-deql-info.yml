info:
  name: 02-deql-info
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/{{deql_org_id}}/deql/info"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
